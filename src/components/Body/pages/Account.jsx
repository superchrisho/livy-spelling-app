import styled from "styled-components";

const StyledAccount = styled.div``;

const Account = () => {
  return (
    <StyledAccount>
      <h1>Account</h1>
    </StyledAccount>
  );
};

export default Account;
